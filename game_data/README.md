# Local game-data cache

Generated versioned Riot Data Dragon catalogs live here and should normally not
be committed.

Use:

```powershell
tft-analyzer sync-game-data --version 16.16.1
```

The active catalog pointer is:

```text
game_data/riot_ddragon/active.json
```
